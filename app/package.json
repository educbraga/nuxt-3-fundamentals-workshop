{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev -o",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.3.3",
    "prettier": "2.8.7",
    "sass": "^1.61.0",
    "sass-loader": "^13.2.2"
  },
  "dependencies": {
    "bulma": "^0.9.4"
  }
}
